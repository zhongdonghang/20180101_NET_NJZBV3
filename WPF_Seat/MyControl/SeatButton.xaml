<UserControl x:Class="WPF_Seat.MyControl.SeatButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="48" d:DesignWidth="48" Loaded="UserControl_Loaded">
    
    <Canvas x:Name="seatElement"  ClipToBounds="True" >
        <Rectangle  x:Name="readerImg" Height="32" Width="32" Canvas.Left="8" Canvas.Top="6"  Fill="{Binding ReaderBackground }" />
        <Rectangle x:Name="powerImg"  Height="16" Width="16" Canvas.Left="0" Canvas.Top="32" Fill="{Binding PowerImg}"/>
        <Rectangle x:Name="shortleaveImg" Height="16" Width="16" Canvas.Left="32" Canvas.Top="0" Fill="{Binding ShowleaveImg}" /> 
        <TextBlock  x:Name="lblSeatNo" Canvas.Top="14" Height="20" Width="48" FontFamily="微软雅黑"  TextAlignment="Center"  FontSize="15" FontWeight="Bold" Canvas.Left="0" Foreground="White" Visibility="Collapsed" ></TextBlock>
    </Canvas>
</UserControl>
